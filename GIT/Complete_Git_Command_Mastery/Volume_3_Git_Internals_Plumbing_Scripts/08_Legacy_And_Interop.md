# Legacy & Interoperability

**Volume 3: Git Internals, Plumbing & Scripts**

## Focus Topics
- SVN
- CVS
- P4

## Command Synopsis
- `git svn` — Bidirectional operation between Subversion and Git
- `git cvsimport` — Salvage your data out of another SCM
- `git p4` — Import from and submit to Perforce repositories
- `git quiltimport` — Applies a quilt patchset onto the current branch

## Fundamental Usage
### `git svn`
- Purpose: Bidirectional operation between Subversion and Git
- Core patterns:
```bash
git svn --help
git svn
```

### `git cvsimport`
- Purpose: Salvage your data out of another SCM
- Core patterns:
```bash
git cvsimport --help
git cvsimport
```

### `git p4`
- Purpose: Import from and submit to Perforce repositories
- Core patterns:
```bash
git p4 --help
git p4
```

### `git quiltimport`
- Purpose: Applies a quilt patchset onto the current branch
- Core patterns:
```bash
git quiltimport --help
git quiltimport
```

