# Rewriting History

**Volume 2: Advanced Management, Auditing & Repair**

## Focus Topics
- Rebase
- Cherry Pick
- Replace

## Command Synopsis
- `git rebase` — Reapply commits on top of another base tip
- `git cherry-pick` — Apply the changes introduced by some existing commits
- `git replace` — Create, list, delete refs to replace objects
- `git filter-branch` — Rewrite branches (Legacy/Powerful)

## Fundamental Usage
### `git rebase`
- Purpose: Reapply commits on top of another base tip
- Core patterns:
```bash
git rebase --help
git rebase
```

### `git cherry-pick`
- Purpose: Apply the changes introduced by some existing commits
- Core patterns:
```bash
git cherry-pick --help
git cherry-pick
```

### `git replace`
- Purpose: Create, list, delete refs to replace objects
- Core patterns:
```bash
git replace --help
git replace
```

### `git filter-branch`
- Purpose: Rewrite branches (Legacy/Powerful)
- Core patterns:
```bash
git filter-branch --help
git filter-branch
```

