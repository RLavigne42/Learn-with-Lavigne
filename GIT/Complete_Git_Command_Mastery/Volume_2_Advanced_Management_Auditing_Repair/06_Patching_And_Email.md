# Patching & Email Workflows

**Volume 2: Advanced Management, Auditing & Repair**

## Focus Topics
- Email based workflow
- Manual patching

## Command Synopsis
- `git format-patch` — Prepare patches for e-mail submission
- `git am` — Apply a series of patches from a mailbox
- `git apply` — Apply a patch to files and/or to the index
- `git request-pull` — Generates a summary of pending changes
- `git send-email` — Send a collection of patches as emails

## Fundamental Usage
### `git format-patch`
- Purpose: Prepare patches for e-mail submission
- Core patterns:
```bash
git format-patch --help
git format-patch
```

### `git am`
- Purpose: Apply a series of patches from a mailbox
- Core patterns:
```bash
git am --help
git am
```

### `git apply`
- Purpose: Apply a patch to files and/or to the index
- Core patterns:
```bash
git apply --help
git apply
```

### `git request-pull`
- Purpose: Generates a summary of pending changes
- Core patterns:
```bash
git request-pull --help
git request-pull
```

### `git send-email`
- Purpose: Send a collection of patches as emails
- Core patterns:
```bash
git send-email --help
git send-email
```

