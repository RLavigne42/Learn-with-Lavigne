# Advanced Search & Comparison

**Volume 2: Advanced Management, Auditing & Repair**

## Focus Topics
- Grepping
- Diff Tools
- Range Diffs

## Command Synopsis
- `git grep` — Print lines matching a pattern
- `git difftool` — Show changes using common diff tools
- `git range-diff` — Compare two commit ranges
- `git describe` — Give an object a human readable name
- `git name-rev` — Find symbolic names for given revs

## Fundamental Usage
### `git grep`
- Purpose: Print lines matching a pattern
- Core patterns:
```bash
git grep --help
git grep
```

### `git difftool`
- Purpose: Show changes using common diff tools
- Core patterns:
```bash
git difftool --help
git difftool
```

### `git range-diff`
- Purpose: Compare two commit ranges
- Core patterns:
```bash
git range-diff --help
git range-diff
```

### `git describe`
- Purpose: Give an object a human readable name
- Core patterns:
```bash
git describe --help
git describe
```

### `git name-rev`
- Purpose: Find symbolic names for given revs
- Core patterns:
```bash
git name-rev --help
git name-rev
```

