# Signatures & Verification

**Volume 2: Advanced Management, Auditing & Repair**

## Focus Topics
- GPG
- Trust

## Command Synopsis
- `git verify-commit` — Check the GPG signature of commits
- `git verify-tag` — Check the GPG signature of tags
- `git verify-pack` — Validate packed Git archive files

## Fundamental Usage
### `git verify-commit`
- Purpose: Check the GPG signature of commits
- Core patterns:
```bash
git verify-commit --help
git verify-commit
```

### `git verify-tag`
- Purpose: Check the GPG signature of tags
- Core patterns:
```bash
git verify-tag --help
git verify-tag
```

### `git verify-pack`
- Purpose: Validate packed Git archive files
- Core patterns:
```bash
git verify-pack --help
git verify-pack
```

