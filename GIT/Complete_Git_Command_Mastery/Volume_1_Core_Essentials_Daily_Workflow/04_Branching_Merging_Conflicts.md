# Branching & Merging

**Volume 1: Core Essentials & Daily Workflow**

## Focus Topics
- Branch Management
- Merging Strategies
- Conflict Resolution

## Command Synopsis
- `git branch` — List, create, or delete branches
- `git switch` — Switch branches
- `git checkout` — Switch branches or restore working tree files
- `git merge` — Join two or more development histories
- `git mergetool` — Run merge conflict resolution tools
- `git diff` — Show changes between commits/trees

## Fundamental Usage
### `git branch`
- Purpose: List, create, or delete branches
- Core patterns:
```bash
git branch --help
git branch
```

### `git switch`
- Purpose: Switch branches
- Core patterns:
```bash
git switch --help
git switch
```

### `git checkout`
- Purpose: Switch branches or restore working tree files
- Core patterns:
```bash
git checkout --help
git checkout
```

### `git merge`
- Purpose: Join two or more development histories
- Core patterns:
```bash
git merge --help
git merge
```

### `git mergetool`
- Purpose: Run merge conflict resolution tools
- Core patterns:
```bash
git mergetool --help
git mergetool
```

### `git diff`
- Purpose: Show changes between commits/trees
- Core patterns:
```bash
git diff --help
git diff
```

