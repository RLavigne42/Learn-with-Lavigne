# Stashing, Tagging & Ignoring

**Volume 1: Core Essentials & Daily Workflow**

## Focus Topics
- Temporary Saves
- Releases
- Excluding Files

## Command Synopsis
- `git stash` — Stash the changes in a dirty working directory away
- `git tag` — Create, list, delete or verify tags
- `git check-ignore` — Debug gitignore / exclude files
- `git check-attr` — Display gitattributes information

## Fundamental Usage
### `git stash`
- Purpose: Stash the changes in a dirty working directory away
- Core patterns:
```bash
git stash --help
git stash
```

### `git tag`
- Purpose: Create, list, delete or verify tags
- Core patterns:
```bash
git tag --help
git tag
```

### `git check-ignore`
- Purpose: Debug gitignore / exclude files
- Core patterns:
```bash
git check-ignore --help
git check-ignore
```

### `git check-attr`
- Purpose: Display gitattributes information
- Core patterns:
```bash
git check-attr --help
git check-attr
```

